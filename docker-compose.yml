services:
    web:
        build: 
            context: .
            dockerfile: ./docker/ui.Dockerfile
        ports:
            - "3000:3000"
    logic:
        build: 
            context: .
            dockerfile: ./docker/logic.Dockerfile
        ports:
            - "5000:5000"